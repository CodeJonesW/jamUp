import React from 'react'
import { useAuth } from "../../Hooks/use-auth";

const CreateJamPage = (props) => {
    const auth = useAuth();
    return (
        <div>
            <input />
        </div>
    )
}

export default CreateJamPage;